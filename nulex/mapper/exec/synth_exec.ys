read_verilog -sv work/exec.v
hierarchy -check -top exec_top
proc; flatten; opt -full
memory_collect; memory_map; opt -full
techmap; opt -full
dfflegalize -cell $_DFF_P_ x
simplemap
opt_clean
write_json work/exec.json
